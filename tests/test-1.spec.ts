import { test } from "@playwright/test";

/**
 * Legacy Playwright codegen recording of the (pre-2.0) manager rating flow.
 *
 * SKIPPED: this was an auto-generated recording against the old EmPortal login
 * (CAPTCHA) and the legacy "Ratings" navigation, both of which no longer exist
 * in EmPortal 2.0. The maintained equivalents are:
 *   - self-rating: emportal_SelfRatingSubmitTest.spec.ts (SelfRatingPage)
 *   - manager rating: emportal_RatingsCreateTest.spec.ts (skipped - needs an
 *     appraiser account)
 * Kept as a skipped placeholder for history; safe to delete.
 */
test.skip("legacy ratings codegen recording (pre-2.0)", async () => {
  // Intentionally skipped - superseded by the self-rating spec.
});
